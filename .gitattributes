# FORK-SPECIFIC FILES - Never pushed to upstream, never overwritten by upstream
# These files exist ONLY in gmkung/web3allowlist fork

# Keep fork workflow during upstream merges
.github/workflows/sync-kleros-allowlist.yml merge=ours

# Keep this .gitattributes during upstream merges  
.gitattributes merge=ours

# Keep updated Kleros sync script (has correct Envio endpoint)
kleros_cdn_to_github.py merge=ours

